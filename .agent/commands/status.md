# /status

Show the Shogun Ops dashboard (`dashboard.md`).

On first run, initialize `agentic-sdd-ops/` under `git rev-parse --git-common-dir`.

## Usage

```
/status
```

## Script

```bash
python3 scripts/shogun-ops.py status
```

## Related

- `.agent/commands/review-cycle.md`
- `scripts/worktree.sh`
